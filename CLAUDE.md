# ACTSET — 공연 특화 AI 홍보물 제작 플랫폼

공연정보를 입력하면 포스터 시안을 생성하고, 확정된 시안을 12종 규격으로 일괄변환하며, 인쇄까지 잇는 서비스. 반달컴퍼니. 개발 인력 2명(Java).

## 단일 진실 소스: docs/

기획은 전부 `docs/00~16` 문서에 있다. **코드를 쓰기 전에 해당 티켓과 관련 문서를 먼저 읽는다.**

| 찾는 것 | 문서 |
|---|---|
| 지금 할 일 (티켓·선행관계·완료기준) | **docs/13-개발티켓.md** |
| 데이터 모델 / DB 스키마 | docs/02, docs/10 |
| API 명세 | docs/11 |
| 화면 동작·상태 | docs/03, docs/04 |
| AI 엔진·레이어 구조 | docs/05 |
| 규격 상수 13종·비율 구간 | docs/12 |
| 크레딧 규칙 | docs/06 |
| UI 디자인 토큰 | docs/16 |
| 외부 API·법무·운영 | docs/14, docs/15 |

문서와 코드가 어긋나는 것을 발견하면 임의로 해석하지 말고 **해당 문서를 함께 고친다** (왕복 허용 — docs/00 진행 원칙).

## 기술 스택 (docs/09 확정)

- **Spring Boot 3 / Java 21 단일 애플리케이션.** `web` / `worker` 프로필로 같은 jar를 나눠 실행. 워커는 jobs 테이블을 `@Scheduled` + `FOR UPDATE SKIP LOCKED`로 폴링
- 이미지 렌더링 **Java2D** (PoC 이식: `poc-java/PosterRenderer.java`), 인쇄 PDF는 PDFBox
- 프런트 **React + TypeScript (Vite)** → 빌드 결과를 Spring Boot `static/`에 내장. API는 `/api/v1`, 세션 쿠키 인증
- PostgreSQL (Flyway 마이그레이션, docs/10 순서 준수), 파일은 S3 호환 스토리지
- 규격은 DB 테이블이 아니라 **코드 상수 13종** + 비율 구간 6개 (docs/12)

## 어겨서는 안 되는 규칙

1. **업로드 사진(출연진·공연 사진)·로고는 외부 AI API로 절대 전송하지 않는다.** 자체 엔진이 PHOTO 레이어로 합성한다. 외부 API 어댑터의 입력 타입에 사진·개인정보 필드를 두지 않는다 (docs/05·15)
2. **텍스트는 이미지에 굽지 않는다.** 모든 텍스트·사진·로고는 레이어(BACKDROP→SUBJECT→DECOR→PHOTO→TITLE→INFO→MARK)로 합성 (docs/05)
3. **레이어 분해는 ④ 확정 시 1회만.** 규격 변환은 저장된 레이어 재배치만 한다
4. **크레딧은 작업 등록 시 차감, 실패 시 환불.** 잔액 갱신·이력·작업 등록은 한 트랜잭션, `CHECK (credit_balance >= 0)` (docs/06·10)
5. **레이아웃 값을 하드코딩하지 않는다.** GenreRule·FormatRule 값은 학습 산출물이다. 임시값은 임시임을 코드에 명시 (docs/05 0-3)
6. **SelectionEvent 적재(1-13)를 미루지 않는다** — 핵심 자산
7. 타인 프로젝트 접근은 `404`, 결과물 URL은 만료되는 서명 URL
8. UI는 docs/16 CSS 변수만 참조한다. hex를 컴포넌트에 직접 쓰지 않는다. **홍보물 결과물에 브랜드 색·워터마크 금지**
9. 외부 AI API는 테스트에서 반드시 목킹한다 — 테스트가 실제 과금을 만들면 안 된다 (P-5)

## 작업 방식

- 티켓(docs/13) 단위로 진행하고, 티켓의 **완료기준**을 확인한 뒤 끝낸 것으로 본다
- 커밋 메시지에 티켓 번호를 붙인다 (예: `[1-7] ① 공연정보 입력 + draft 생성`)
- 시크릿(API 키·DB 비밀번호)은 코드·저장소에 절대 넣지 않는다 (P-2)
