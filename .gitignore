# Claude Code 개인 설정 (밤새 자동진행 권한 설정 등 — 팀원과 공유하지 않음)
.claude/settings.local.json

# 시크릿
.env
.env.local
.env.*.local

# Java / Spring Boot
target/
build/
*.class
.gradle/
HELP.md

# Node / React
node_modules/
dist/
.vite/
*.tsbuildinfo

# 로그·IDE
*.log
.idea/
.vscode/

# 로컬 포터블 툴체인 (JDK21·Maven·PostgreSQL — 개발 편의용, 저장소에 넣지 않음)
tools/

# 로컬 파일 스토리지 (S3 대체 — StorageService 로컬 구현 산출물)
/storage/

# 프런트 빌드 산출물이 백엔드 static/으로 복사되기 전 중간 산출물
frontend/dist/
